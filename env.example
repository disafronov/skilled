#
# Skilled — environment variables example
#
# Copy to `.env` (optional) and adjust values as needed.
# The application loads env vars via `os.getenv(...)`.
#

# -----------------------------------------------------------------------------
# Django
# -----------------------------------------------------------------------------

# Debug mode. Set to False in production.
DJANGO_DEBUG=True

# Django secret key. Generate a strong random value for production:
#   python -c "import secrets; print(secrets.token_urlsafe(50))"
DJANGO_SECRET_KEY=insecure-dev-key-change-in-production

# Comma-separated list of allowed hostnames/IPs.
# Use "*" only behind a trusted reverse proxy.
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost

# Security flags. Defaults are DEBUG-aware; set explicitly in production.
DJANGO_SECURE_SSL_REDIRECT=0
DJANGO_SESSION_COOKIE_SECURE=0
DJANGO_CSRF_COOKIE_SECURE=0

# Admin superuser (created by `make run` and `make docker-run`).
# If set, the admin user is created automatically on startup.
DJANGO_SUPERUSER_USERNAME=admin
DJANGO_SUPERUSER_EMAIL=admin@example.com
# Must satisfy AUTH_PASSWORD_VALIDATORS: minimum length 8, not a common
# password, not entirely numeric, and not similar to the username.
DJANGO_SUPERUSER_PASSWORD=ChangeMe-In-Prod-123!

# -----------------------------------------------------------------------------
# Encryption
# -----------------------------------------------------------------------------

# AES-SIV key for deterministic encryption of sensitive fields at rest
# (telegram_api_token, auth_token, etc). Generate one with:
#   python -c "import secrets; print(secrets.token_bytes(32).hex())"
# Dev key (change in production — any data encrypted with the old key
# will be lost on rotation).
FIELD_ENCRYPTION_KEY=b266e679c9ca7460e3906bea5fb538c4822d41ccb7a3796c0d85e374d0281cdb

# -----------------------------------------------------------------------------
# LLM
# -----------------------------------------------------------------------------

# Optional policy file appended to every LLM SKILL + Wrapper combo.
# Missing or empty file means no global policy.
POLICY_FILE=policy.md

# -----------------------------------------------------------------------------
# Telegram
# -----------------------------------------------------------------------------

# Public HTTPS base URL used to register Telegram webhooks.
# Leave empty to use polling only.
DJANGO_BASE_URL=

# Wait this many seconds before retrying a webhook after polling fallback.
WEBHOOK_COOLDOWN_SECONDS=300

# Fall back to polling when Telegram reports more pending webhook updates.
WEBHOOK_FALLBACK_PENDING_THRESHOLD=5

# Emoji reaction for queue acknowledgement (empty string to disable).
TELEGRAM_ACK_REACTION=🤔

# Group Telegram messages into one buffer if they arrive within this many seconds.
TELEGRAM_INTAKE_DEBOUNCE_SECONDS=10

# -----------------------------------------------------------------------------
# PostgreSQL
# -----------------------------------------------------------------------------

# Database connection settings.
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=database
DATABASE_USER=user
DATABASE_PASSWORD=password

# -----------------------------------------------------------------------------
# Django Q2
# -----------------------------------------------------------------------------

# Task queue cluster name.
Q_CLUSTER_NAME=skilled
# Number of worker processes.
Q_WORKERS=2
# Task timeout in seconds.
Q_TIMEOUT=600
# Retry timeout in seconds (must be > Q_TIMEOUT).
Q_RETRY=660
# Maximum number of queued tasks.
Q_QUEUE_LIMIT=50
# Number of tasks to fetch in bulk.
Q_BULK=10

# -----------------------------------------------------------------------------
# Django Q2 task schedules (managed in code and overwritten in the DB)
# -----------------------------------------------------------------------------
# Values are minutes between runs.

# How often to manage Telegram webhook setup and fallback.
Q2_TELEGRAM_SETUP_MINUTES=1

# How often to poll Telegram for new messages.
Q2_TELEGRAM_INGEST_MINUTES=1

# How often to check for pending LLM jobs.
Q2_PROCESSING_MINUTES=1

# Requeue LLM jobs that were started but not finished after this many seconds.
Q2_PROCESSING_STALE_JOB_SECONDS=3600

# How often to deliver completed responses to Telegram.
Q2_TELEGRAM_DELIVER_MINUTES=1

# Retry delivery attempts left unfinished for longer than this many seconds.
Q2_DELIVERY_STALE_JOB_SECONDS=3600

# How often to delete stale successful django-q2 task records.
Q2_SUCCESS_CLEANUP_MINUTES=60

# How often to flush stale intake buffers.
Q2_TELEGRAM_INTAKE_FLUSH_MINUTES=1

# Delete successful django-q2 task records after this many seconds.
Q2_SUCCESS_RETENTION_SECONDS=86400

# Graceful shutdown window (seconds) before SIGKILL of child processes.
GRACEFUL_TIMEOUT=25
